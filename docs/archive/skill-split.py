# A simple splitter that INTELLIGENTLY splits YAML and MARKDOWN by section and subsections, so that each SECTION is stored in a simple db under the <skill_name>. This will allow for real progressive disclosure

# Written in python to ensure the PROGRAM verifies what the LLM 'claims' This program assumes errors, so as to avoide them, and keeps it simple.
